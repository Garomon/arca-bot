const fs=require(" fs\);const D=\/root/arca-bot/data/sessions\;fs.readdirSync(D).forEach(f=>{if(!f.endsWith(\_state.json\))return;if(f.includes(\backup\))return;try{const s=JSON.parse(fs.readFileSync(D+\/\+f));const I=s.inventory||[];const amt=I.reduce((a,b)=>a+(b.remaining||0),0);const cost=I.reduce((a,b)=>a+((b.price||0)*(b.remaining||0)),0);const avg=amt?cost/amt:0;console.log(s.pair,s.currentPrice,avg,s.dcaBlockStartTime||0);}catch(e){}});
